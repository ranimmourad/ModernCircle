import LoginPlaceholder from "@/components/LoginPlaceholder";

export const metadata = { title: "Employee Login — Modern Circle" };

export default function EmployeeLoginPage() {
  return <LoginPlaceholder title="Employee Login" />;
}
