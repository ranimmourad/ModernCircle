import LoginPlaceholder from "@/components/LoginPlaceholder";

export const metadata = { title: "Admin Login — Modern Circle" };

export default function AdminLoginPage() {
  return <LoginPlaceholder title="Admin Login" />;
}
